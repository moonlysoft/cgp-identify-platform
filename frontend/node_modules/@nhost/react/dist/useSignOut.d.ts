/**
 * Use the hook `useSignOut` to sign out the user.
 *
 * @example
 * ```tsx
 * import { useSignOut, useAuthenticated } from '@nhost/react'
 *
 * const Component = () => {
 *   const { signOut } = useSignOut()
 *   const isAuthenticated = useAuthenticated()
 *
 *   if (isAuthenticated) {
 *     return (
 *       <button onClick={() => signOut()}>Sign Out</button>
 *     )
 *   }
 *
 *   return <div>Not authenticated</div>
 * }
 * ```
 *
 * @docs https://docs.nhost.io/reference/react/use-sign-out
 */
export declare const useSignOut: (stateAll?: boolean) => {
    signOut: (valueAll?: boolean | unknown) => Promise<import("@nhost/core").SignOutlessHandlerResult>;
    isSuccess: boolean;
    error: import("@nhost/core").ErrorPayload | null;
};
//# sourceMappingURL=useSignOut.d.ts.map